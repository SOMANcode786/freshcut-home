import {prisma} from '../config/prisma.js';
export const Product={all(){return prisma.product.findMany({orderBy:{id:'asc'}})},find(id){return prisma.product.findUnique({where:{id:Number(id)}})},update(id,changes){const allowed=['name','slug','urdu','cat','icon','image','description','prices','sale','active'];const data=Object.fromEntries(Object.entries(changes).filter(([key])=>allowed.includes(key)));return prisma.product.update({where:{id:Number(id)},data})}};
